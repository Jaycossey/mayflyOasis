// Gallery component, will take props as a randomly assigned artist object to handle the data displayed.
const Gallery = (props) => {

    const artistData = props.artistPreview;
    return (
        <div>
            {/* <img /> */}
            <h1>Hello World!</h1>
            <p>Example Text</p>
        </div>
    );
}

export default Gallery;